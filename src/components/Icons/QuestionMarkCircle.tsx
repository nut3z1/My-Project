import type { FC, SVGProps } from "react";

import Icon from "../../../public/question-mark-circle.svg";

export const QuestionMarkCircle: FC<SVGProps<SVGSVGElement>> = (props) => (
  <Icon {...props} />
);
